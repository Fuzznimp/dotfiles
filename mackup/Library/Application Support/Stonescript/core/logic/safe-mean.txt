var speedrun = true
// NOTE: Allows smite stacking.
var extra_frames = 10

// NOTE: `pickup.distance > 20` allows to pick up the chest and leave afterwards if needed.
func handle()
  ?speedrun
    ?((loc.averageTime + extra_frames) < totaltime & pickup.distance > 20) & loc.averageTime > 0
      loc.Leave()
